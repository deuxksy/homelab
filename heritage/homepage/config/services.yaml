# Homepage Services Configuration - The Heritage Collection
# https://gethomepage.dev/configs/services/

- Info:
    - Calendar:
        widget:
          type: calendar
          firstDayInWeek: sunday
          view: monthly
          maxEvents: 10
          showTime: true

- Media:
    - Transmission:
        label: "Torrent Node"
        icon: transmission.png
        href: https://heritage.bun-bull.ts.net/transmission/web/
        container: transmission
        widget:
          type: transmission
          url: http://localhost:8091

- Streaming:
    - Jellyfin:
        label: "Media Server"
        icon: jellyfin.png
        href: https://heritage.bun-bull.ts.net/jellyfin
        container: jellyfin
        widget:
          type: jellyfin
          # BaseUrl=/jellyfin 설정이라 위젯 URL에도 base path 필수 (/emby/*가 302됨)
          url: http://localhost:8096/jellyfin
          key: "{{HOMEPAGE_VAR_KEY_JELLYFIN}}"
          enableBlocks: true
          enableNowPlaying: true
          enableUser: true
          showEpisodeNumber: true
          expandOneStreamToTwoRows: false

- Photos:
    - Immich:
        label: "Photo Gallery"
        icon: immich.png
        href: https://heritage.bun-bull.ts.net:2283
        container: immich_server
        # 위젯 미사용 — homepage(v2.1.2)가 구 API 경로(/api/server-info/*)를 호출해
        # Immich v3(경로 개명 /api/server/*)와 호환 안 됨. 상류 지원 시 재활성화
